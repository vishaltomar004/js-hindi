function logger(username)
{
    return `${username} just logged in`;
}
console.log(logger("vivek"));

//  nfunction with infinite parameter

function calculateCartPrice(num1)
{
    return num1;
}

const user = {
    username : "Vishal",
    price : 99
}

function handleObject(anyObject) {
console.log(`User name is ${anyObject.username} ` );

}

handleObject(user)

